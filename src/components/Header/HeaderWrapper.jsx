import ClassicHeader from "./ClassicHeader";

const HeaderWrapper = ({ classicHeader, handleNavClick }) => {
  return <ClassicHeader handleNavClick={handleNavClick} />;
};

export default HeaderWrapper;
